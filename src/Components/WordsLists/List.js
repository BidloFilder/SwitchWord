import React, { useState } from 'react';

const List = (props) => {
  const [wordValue, setWordValue] = useState(props.wordValue)
  const [inputValue, setInputValue] = useState(props.inputValue)
  
  
    return (
    <div>
      
    </div>
  );
};

export default List;
